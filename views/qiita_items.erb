<p>Qiitaの投稿一覧を<a href='https://qiita.com/docs'>API</a>から取得してみる</p>

<p><%= "#{items.size}件" %></p>

<ul>
	<% items.each do |item| %>
		<li>
			<a href="<%= item[:url] %>" title="">
				<%= item[:title] %>
			</a>
			<ul>
				<li>
					<%= "最終更新 : #{item[:updated_at_in_words]}前" %>
				</li>
				<li>
					tags : [<%= item[:tags].map{|v| v[:name]}.compact.join(', ') %>]
				</li>
			</ul>
		</li>
	<% end %>
</ul>
<hr>
<div>
	<p><%= "Can request the number of remaining #{remaining} / limit #{limit}" %></p>
</div>
